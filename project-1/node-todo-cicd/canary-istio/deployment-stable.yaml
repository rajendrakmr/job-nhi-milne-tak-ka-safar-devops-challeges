apiVersion: apps/v1
kind: Deployment
metadata:
  name: stable-nodejs-deployment
  namespace: nodejs
spec:
  replicas: 2
  selector:
    matchLabels:
      app: myapp
      version: stable 
  template:
    metadata:
      labels:
        app: myapp
        version: stable 
    spec:
      containers:
      - name: stablemyapp
        image: rk0617/nodeapp-mini:v1.1.0
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
          requests:
            memory: "64Mi"
            cpu: "200m"
        ports:
        - containerPort: 8000
